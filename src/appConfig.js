/** Development Configuration */

const devServicesOrigin = `http://localhost:3000`
const devFrontendOrigin = `http://localhost:4000`

export const appConfig = {
  devFrontendOrigin,
  devServicesOrigin,
}
